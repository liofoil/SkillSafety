# Python 3.12 environment used for artifact-side scripts.
# Versions are recovered from the experiment environment.
openai==1.99.9
daytona==0.128.1

# Harbor supplies the CLI and sandbox/agent integrations. OpenClaw is loaded
# from the vendored adapter in poise_harbor/ via --agent-import-path.
harbor==0.3.0
